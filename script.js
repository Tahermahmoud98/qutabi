const translations = {
    ku_ba: {
        title: "لێگەڕیان ل دیڤ خوشک و برایان و کەسوکاران و قوتابیێن ب تنێ",
        subtitle: "کەشفکرنا خویشک و برایێن ئێک باب، و جوداکرنا مرۆڤان ب پشتبەستن ل سەر باپیرێ 3 و 4 یان ناڤێ دەیکێ",
        input_label: "1. داخلكرنا داتایان",
        top_sidebar_btn: "بەشێ داخلكرنێ ☰",
        mob_sidebar_toggle: "داخلكرن و کۆنترۆڵ ☰",
        mob_paste_tab: "نڤیسین",
        data_status_title: "دۆخێ داتایان:",
        data_status_ready: "داتا یا ئامادەیە",
        btn_open_editor: "نڤیسین",
        btn_upload_file: "ئێکسل",
        modal_title: "داخلكرن و دەستکاریکرنا داتایێن قوتابیان",
        modal_tab_upload: "ئێکسل",
        modal_tab_paste: "نڤیسین / دەق",
        modal_save_detect: "پاشەکەفت و شیکار",
        modal_close: "داخستن",
        modal_clear: "پاککرن",
        instructions_title: "چەوانیا داخلكرنا داتایان / مەلەفێ ئێکسل:",
        instruction_1: "تۆ دشێی مەلەفێ إکسل (XLSX, XLS, CSV, TXT) بهاڤێژیە ڤێرێ یان کلیک بکە بۆ هەلبژارتنا مەلەفی ژ ئامیرێ خۆ.",
        example_1: "ناڤێ قوتابی (سێیانە/چوارانە)، ناڤێ دەیکێ (ب بژاردەیی)، پۆل، شعبە",
        placeholder_input: "ناڤێ قوتابی، ناڤێ دەیکێ (ب بژاردەیی)، پۆل، شعبە ل ڤێرێ ب لکینه...\nنموونە ب ناڤێ دەیکێ:\nئەحمەد عەلی حەسەن محەمەد، لەیلا عومەر فەتاح، دوو، ج\nسارا عەلی حەسەن محەمەد، لەیلا عومەر فەتاح، چار، د\n\nیان بێی ناڤێ دەیکێ:\nخالد ولید حسن محمد، دوو، ج",
        sep_label: "جوداکەر:",
        sep_auto: "ئۆتۆماتیک",
        sep_tab: "Tab",
        sep_comma: "فاریزە ( , )",
        sep_semicolon: "خاڵدار ( ; )",
        match_label: "هەڤبەری:",
        match_rest: "پاشماوە",
        match_last2: "دوو ناڤ",
        match_family: "بنەمالە",
        relatives_mode_label: "رێکا مرۆڤان:",
        relatives_badge: "مرۆڤ",
        grandpa_level_label: "رێکا دیتنا باپیران:",
        paternal_mode: "🌿 باپیرێ 3 و 4 (باب)",
        maternal_mode: "🌸 ناڤێ دەیکێ",
        both_mode: "🔀 باب و دەیک پێکڤە",
        grandpa_3_4: "باپیرێ 3 و 4 (پێشنیارکری)",
        grandpa_3: "باپیرێ 3 بتنێ",
        grandpa_4: "باپیرێ 4 / لەقەب",
        all_ancestors: "هەمی باپیر",
        prefix_label: "پێشگر و (ال)",
        sort_default: "زێدەترین",
        sort_alpha: "ئەبجەدی",
        btn_detect: "دیارکرن",
        btn_excel: "CSV",
        btn_singletons: "ب تنێ",
        btn_print: "چاپ",
        btn_clear: "پاککرن",
        history_label: "ئەرشیف:",
        history_ver: "کۆپیا",
        tab_siblings: "خویشک و برا",
        tab_relatives: "پسمام و کەسوکار",
        tab_solitary: "ب تنێ (بێ مرۆڤ)",
        all_grades: "هەمی قۆناغ",
        relatives_tab_desc: "بەشێ مرۆڤان بتنێ پسمام و کەسوکاران (ژ بابێن جودا یان دەیکێن جودا) نیشان ددەت و خویشک و برایێن ئێک باب ل ڤێرێ نینن.",
        solitary_tab_desc: "ئەڤ بەشە وان قوتابیان نیشان ددەت یێن کو چو خویشک و برایێن ئێک باب و چو پسمام و کەسوکار ل قوتابخانێ نینن (ب تەواوی ب تنێ).",
        placeholder_search: "گەڕیانا بلەز...",
        results_default_desc: "تکایە کلیک ل سەر زرێ '☰' یان 'نڤیسین' بکە بۆ داخلكرنا داتایان، پاشان کلیک ل سەر دیارکرنێ بکە.",
        results_empty_title: "چو ئەنجام نەهاتنە دیتن",
        results_empty_desc: "تکایە پشتڕاست ببە کو تە ناڤ دروست نڤیسینە یان رێکا لێگەڕیانێ بگوهۆڕە.",
        badge_groups_sib: "خێزانێن خویشک و برا",
        badge_groups_rel: "کۆمێن پسمام و کەسوکاران",
        badge_groups_sol: "قوتابیێن ب تنێ (بێ مرۆڤ)",
        family_label: "بنەمالە / باپیرێ هەڤپشک:",
        maternal_family_label: "دەیکا هەڤپشک:",
        siblings_count: "قوتابی:",
        grade_label: "پۆل:",
        section_label: "شعبە:",
        mother_label: "دەیک:",
        footer_text: "🔒 هەمی کردار د ناڤا مۆبایل یان کۆمپیوتەرا تە دا ب شێوەیەکێ پاراستی دهێنە ئەنجامدان.",
        stat_total: "سەرجەمێ قوتابیان",
        stat_siblings: "خویشک و برا",
        stat_relatives: "پسمام و کەسوکار",
        stat_twins: "دووانە",
        stat_solitary: "ب تنێ (بێ مرۆڤ)",
        dark_mode_dark: "تاری",
        dark_mode_light: "رووناهی",
        twin_warn: "⚠️ دووانە؟",
        father_branch: "تایا بابێ: ",
        branch_label: "تاعێ بابێ / لق:",
        students_suffix: "قوتابی",
        groups_suffix: "کۆم",
        copy_btn: "📋 کۆپی",
        copy_success: "کارت ب سەرکەفتی هاتە کۆپیکرن!",
        singletons_title: "لیستا قوتابیێن ب تنێ (بێ خویشک، برا و مرۆڤ):",
        no_singletons: "چو قوتابیێن ب تنێ نینن",
        singletons_count_suffix: "قوتابیێن ب تنێ",
        csv_paternal_type: "ژ لایێ بابێ (باپیرێ 3 و 4)",
        csv_maternal_type: "ژ لایێ دەیکێ",
        csv_solitary_type: "ب تنێ (بێ خویشک و بێ مرۆڤ)",
        csv_type: "جۆرێ مرۆڤایەتیێ",
        csv_shared: "باپیرێ هەڤپشک / دەیک",
        csv_branch: "تاع / باب",
        csv_name: "ناڤێ قوتابی",
        paternal_card_title: "قرابەتا ژ لایێ بابێ (باپیرێ 3 و 4):",
        maternal_card_title: "قرابەتا ژ لایێ دەیکێ (دەیکا هەڤپشک):",
        btn_install_app: "دانانا بەرنامەی",
        dir: "rtl"
    },
    ku_so: {
        title: "دۆزینەوەی خوشک و برا و خزمەکان",
        subtitle: "دۆزینەوەی خوشک و برا و ئامۆزا بە پشت بەستن بە باپیرەی 3 و 4 یان ناوی دایک",
        input_label: "1. داخلکردنی داتا",
        top_sidebar_btn: "بەشی داخلکردن ☰",
        mob_sidebar_toggle: "داخلکردن و کۆنترۆڵ ☰",
        mob_paste_tab: "نووسین",
        data_status_title: "دۆخی داتا:",
        data_status_ready: "داتا ئامادەیە",
        btn_open_editor: "نووسین",
        btn_upload_file: "ئێکسڵ",
        modal_title: "داخلکردن و دەستکاریکردنی زانیارییەکان",
        modal_tab_upload: "ئێکسڵ",
        modal_tab_paste: "نووسین / دەق",
        modal_save_detect: "پاشەکەوت و دۆزینەوە",
        modal_close: "داخستن",
        modal_clear: "سڕینەوە",
        instructions_title: "چۆنیەتی داخلکردنی زانیارییەکان / مەلەفی ئێکسڵ:",
        instruction_1: "دەتوانیت مەلەفی إکسڵ (XLSX, XLS, CSV, TXT) بهاوێژیتە ئێرە یان کلیک بکە بۆ دیاریکردنی مەلەف لە ئامێرەکەت.",
        example_1: "ناوی قوتابی (سێیانە/چوارانە)، ناوی دایک (بە بژاردەیی)، پۆل، شعبە",
        placeholder_input: "ناوی قوتابی، ناوی دایک (بە بژاردەیی)، پۆل، شعبە لێرە لک بدە...\nنموونە بە ناوی دایک:\nئەحمەد عەلی حەسەن محەمەد، لەیلا عومەر فەتاح، دووەم، ج\nسارا عەلی حەسەن محەمەد، لەیلا عومەر فەتاح، چوارەم، د\n\nیان بەبێ ناوی دایک:\nخالید وەلید حەسەن محەمەد، دووەم، ج",
        sep_label: "جیاکەرەوە:",
        sep_auto: "ئۆتۆماتیک",
        sep_tab: "Tab",
        sep_comma: "فاریزە ( , )",
        sep_semicolon: "خاڵبەندی ( ; )",
        match_label: "هاوتاکردن:",
        match_rest: "پاشماوە",
        match_last2: "دوو ناو",
        match_family: "بنەماڵە",
        relatives_mode_label: "ڕێگای خزمەکان:",
        relatives_badge: "خزم",
        grandpa_level_label: "شێوازی باپیرەکان:",
        paternal_mode: "🌿 باپیرەی 3 و 4 (باوک)",
        maternal_mode: "🌸 ناوی دایک",
        both_mode: "🔀 هەردوو لایەن پێکەوە",
        grandpa_3_4: "ناوی 3 و 4 (پێشنیازکراو)",
        grandpa_3: "ناوی 3 تەنها",
        grandpa_4: "ناوی 4 / نازناو",
        all_ancestors: "هەموو باپیرەکان",
        prefix_label: "پێشگر و (ال)",
        sort_default: "زۆرترین",
        sort_alpha: "ئەبجەدی",
        btn_detect: "دۆزینەوە",
        btn_excel: "CSV",
        btn_singletons: "تەنیا",
        btn_print: "چاپ",
        btn_clear: "سڕینەوە",
        history_label: "ئەرشیف:",
        history_ver: "کۆپیی",
        tab_siblings: "خوشک و برا",
        tab_relatives: "ئامۆزا و خزم",
        tab_solitary: "تەنیا (بێ خزم)",
        all_grades: "هەموو قۆناغەکان",
        relatives_tab_desc: "بەشی خزمەکان تەنها ئامۆزا و خزمانی باوک یان دایک (لە باوکی جیاواز) پیشان دەدات.",
        solitary_tab_desc: "ئەم بەشە ئەو قوتابیانە پیشان دەدات کە هیچ خوشک، برا و خزمێکیان لە قوتابخانەدا نییە.",
        placeholder_search: "گەڕانی خێرا...",
        results_default_desc: "تکایە کلیک لەسەر '☰' یان 'نووسین' بکە بۆ داخلکردنی داتا، پاشان کلیک لەسەر دۆزینەوە بکە.",
        results_empty_title: "هیچ ئەنجامێک نەدۆزرایەوە",
        results_empty_desc: "تکایە دڵنیابەوە لەوەی ناوەکانت بە دروستی نووسیوە یان ڕێگای گەڕان بگۆڕە.",
        badge_groups_sib: "خێزانەکانی خوشک و برا",
        badge_groups_rel: "کۆمەڵەی ئامۆزا و خزمەکان",
        badge_groups_sol: "قوتابیانی تەنیا",
        family_label: "بنەماڵە / باپیرەی هاوبەش:",
        maternal_family_label: "دایکی هاوبەش:",
        siblings_count: "قوتابی:",
        grade_label: "پۆل:",
        section_label: "شعبە:",
        mother_label: "دایک:",
        footer_text: "🔒 هەموو کردارەکان لە ناو مۆبایل یان کۆمپیوتەرەکەتدا بە شێوازێکی پارێزراو ئەنجام دەدرێن.",
        stat_total: "کۆی گشتی قوتابیان",
        stat_siblings: "خوشک و برا",
        stat_relatives: "ئامۆزا و خزم",
        stat_twins: "دووانە",
        stat_solitary: "تەنیا (بێ خزم)",
        dark_mode_dark: "تاریک",
        dark_mode_light: "ڕووناک",
        twin_warn: "⚠️ دووانە؟",
        father_branch: "لقی باوک: ",
        branch_label: "لقی باوک / سەرپەرشتیار:",
        students_suffix: "قوتابی",
        groups_suffix: "کۆمەڵە",
        copy_btn: "📋 کۆپی",
        copy_success: "کارت بە سەرکەوتوویی کۆپیکرا!",
        singletons_title: "لیستی قوتابیانی تەنیا (بێ خوشک، برا و خزم):",
        no_singletons: "هیچ قوتابییەکی تەنیا نییە",
        singletons_count_suffix: "قوتابی تەنیا",
        csv_paternal_type: "لە لایەنی باوک (ناوی 3 و 4)",
        csv_maternal_type: "لە لایەنی دایک",
        csv_solitary_type: "تەنیا (بێ خوشک و بێ خزم)",
        csv_type: "جۆری خزمایەتی",
        csv_shared: "باپیرەی هاوبەش / دایک",
        csv_branch: "لق / باوک",
        csv_name: "ناوی قوتابی",
        paternal_card_title: "خزمایەتی لە لایەنی باوک (ناوی 3 و 4):",
        maternal_card_title: "خزمایەتی لە لایەنی دایک (دایکی هاوبەش):",
        btn_install_app: "دابەزاندنی بەرنامە",
        dir: "rtl"
    },
    ar: {
        title: "كاشف الإخوة والأقارب في المدرسة",
        subtitle: "كشف الإخوة الأشقاء بدقة، وفصل الأقارب بناءً على الأسماء 2 و 3 و 4 أو اسم الأم",
        input_label: "1. إدخال البيانات",
        top_sidebar_btn: "لوحة التحكم ☰",
        mob_sidebar_toggle: "الإدخال والتحكم ☰",
        mob_paste_tab: "كتابة",
        data_status_title: "حالة البيانات:",
        data_status_ready: "البيانات جاهزة",
        btn_open_editor: "كتابة",
        btn_upload_file: "إكسل",
        modal_title: "إدخال وتعديل بيانات الطلاب",
        modal_tab_upload: "إكسل",
        modal_tab_paste: "كتابة / نص",
        modal_save_detect: "حفظ وتحليل",
        modal_close: "إغلاق",
        modal_clear: "مسح",
        instructions_title: "طريقة إدخال البيانات / رفع ملف إكسل:",
        instruction_1: "يمكنك سحب وإفلات أي ملف إكسل (XLSX, XLS, CSV, TXT) هنا أو الضغط لاختيار ملف من جهازك.",
        example_1: "اسم الطالب (ثلاثي أو رباعي)، اسم الأم (اختياري)، الصف، الشعبة",
        placeholder_input: "الصق أو اكتب: اسم الطالب، اسم الأم (اختياري)، الصف، الشعبة...\nمثال بوجود اسم الأم:\nأحمد علي حسن محمد، ليلى عمر عبد الله، الثاني، ج\nسارة علي حسن محمد، ليلى عمر عبد الله، الرابع، د\n\nأو بدون اسم الأم:\nخالد وليد حسن محمد، الثاني، ج",
        sep_label: "فاصل:",
        sep_auto: "تلقائي",
        sep_tab: "Tab",
        sep_comma: "فاصلة ( , )",
        sep_semicolon: "منقوطة ( ; )",
        match_label: "مطابقة:",
        match_rest: "باقي الاسم",
        match_last2: "آخر اسمين",
        match_family: "العائلة",
        relatives_mode_label: "مسار القرابة:",
        relatives_badge: "أقارب",
        grandpa_level_label: "طريقة الأجداد:",
        paternal_mode: "🌿 جهة الأب (الجد 3 و 4)",
        maternal_mode: "🌸 جهة الأم",
        both_mode: "🔀 كلاهما معاً",
        grandpa_3_4: "الجد 3 و 4 (موصى به)",
        grandpa_3: "الجد 3 فقط",
        grandpa_4: "الجد 4 / اللقب",
        all_ancestors: "كل الأجداد",
        prefix_label: "السوابق و (ال)",
        sort_default: "الأكثر",
        sort_alpha: "أبجدياً",
        btn_detect: "كشف",
        btn_excel: "CSV",
        btn_singletons: "وحيدين",
        btn_print: "طباعة",
        btn_clear: "مسح",
        history_label: "الأرشيف:",
        history_ver: "نسخة",
        tab_siblings: "الإخوة الأشقاء",
        tab_relatives: "الأقارب والعمومة",
        tab_solitary: "وحيدين (بدون أقارب)",
        all_grades: "كل المراحل",
        relatives_tab_desc: "قسم الأقارب يعرض فقط الطلاب المرتبطين كأبناء عمومة أو خؤولة (من آباء مختلفين / أمهات مختلفات) مع استبعاد الإخوة المباشرين.",
        solitary_tab_desc: "هذا القسم يعرض الطلاب الذين ليس لديهم أي إخوة أشقاء ولا أبناء عمومة أو خؤولة مسجلين في المدرسة (وحيدون تماماً).",
        placeholder_search: "بحث سريع...",
        results_default_desc: "يرجى الضغط على زر '☰' أو 'كتابة' لإدخال القائمة، ثم الضغط على زر الكشف.",
        results_empty_title: "لم يتم العثور على مجموعات",
        results_empty_desc: "يرجى التأكد من كتابة الأسماء الكاملة أو تجربة طريقة مطابقة أخرى.",
        badge_groups_sib: "عائلات الإخوة الأشقاء",
        badge_groups_rel: "مجموعات الأقارب والعمومة",
        badge_groups_sol: "طلاب وحيدين تماماً",
        family_label: "الجد المشترك / العائلة:",
        maternal_family_label: "الأم المشتركة:",
        siblings_count: "طلاب:",
        grade_label: "الصف:",
        section_label: "الشعبة:",
        mother_label: "الأم:",
        footer_text: "🔒 جميع العمليات تتم داخل المتصفح بأمان كامل للحفاظ على خصوصية بيانات الطلاب.",
        stat_total: "إجمالي الطلاب",
        stat_siblings: "الإخوة الأشقاء",
        stat_relatives: "الأقارب",
        stat_twins: "التوائم",
        stat_solitary: "وحيدين (بدون أقارب)",
        dark_mode_dark: "داكن",
        dark_mode_light: "فاتح",
        twin_warn: "⚠️ توأم؟",
        father_branch: "فرع الأب: ",
        branch_label: "فرع الأب / ولي الأمر:",
        students_suffix: "طلاب",
        groups_suffix: "مجموعات",
        copy_btn: "📋 نسخ",
        copy_success: "تم نسخ البطاقة بنجاح!",
        singletons_title: "قائمة الطلاب الوحيدين تماماً (لا إخوة ولا أقارب في المدرسة):",
        no_singletons: "لا يوجد طلاب وحيدين",
        singletons_count_suffix: "طلاب وحيدين",
        csv_paternal_type: "جهة الأب (الاسم 3 و 4)",
        csv_maternal_type: "جهة الأم",
        csv_solitary_type: "وحيد تماماً (بدون إخوة وبدون أقارب)",
        csv_type: "نوع القرابة",
        csv_shared: "الجد المشترك / الأم",
        csv_branch: "الفرع / الأب",
        csv_name: "اسم الطالب",
        paternal_card_title: "قرابة من جهة الأب (الاسم 3 و 4):",
        maternal_card_title: "قرابة من جهة الأم (الأم المشتركة):",
        btn_install_app: "تثبيت التطبيق",
        dir: "rtl"
    },
    en: {
        title: "School Siblings & Relatives Detector",
        subtitle: "Accurately detect direct siblings and isolate cousins based on 2nd, 3rd & 4th names or mother name",
        input_label: "1. Data Entry",
        top_sidebar_btn: "Sidebar ☰",
        mob_sidebar_toggle: "Sidebar ☰",
        mob_paste_tab: "Write",
        data_status_title: "Data Status:",
        data_status_ready: "Data Ready",
        btn_open_editor: "Write",
        btn_upload_file: "Excel",
        modal_title: "Student Data Entry & Upload",
        modal_tab_upload: "Excel",
        modal_tab_paste: "Write / Text",
        modal_save_detect: "Save & Detect",
        modal_close: "Close",
        modal_clear: "Clear",
        instructions_title: "How to enter data / Upload Excel:",
        instruction_1: "Drag and drop any Excel file (XLSX, XLS, CSV, TXT) here or click to select from your device.",
        example_1: "Student Name (3-4 parts), Mother's Name (optional), Grade, Section",
        placeholder_input: "Paste or type: Student Name, Mother Name (optional), Grade, Section...\nExample with Mother:\nAhmed Ali Hassan Mohammed, Layla Omar Abdullah, 2nd, C\nSara Ali Hassan Mohammed, Layla Omar Abdullah, 4th, D\n\nOr without Mother:\nKhaled Waleed Hassan, 2nd, C",
        sep_label: "Separator:",
        sep_auto: "Auto",
        sep_tab: "Tab",
        sep_comma: "Comma ( , )",
        sep_semicolon: "Semicolon ( ; )",
        match_label: "Match:",
        match_rest: "Rest Name",
        match_last2: "Last 2 Names",
        match_family: "Family",
        relatives_mode_label: "Relatives Mode:",
        relatives_badge: "Relatives",
        grandpa_level_label: "Ancestors Depth:",
        paternal_mode: "🌿 3rd & 4th Grandpas (Father)",
        maternal_mode: "🌸 Via Mother Name",
        both_mode: "🔀 Both Combined",
        grandpa_3_4: "3rd & 4th (Default)",
        grandpa_3: "3rd Name Only",
        grandpa_4: "4th Name / Clan",
        all_ancestors: "All Ancestors",
        prefix_label: "Prefixes & (Al-)",
        sort_default: "Max First",
        sort_alpha: "Alphabetical",
        btn_detect: "Detect",
        btn_excel: "CSV",
        btn_singletons: "Solitary",
        btn_print: "Print",
        btn_clear: "Clear",
        history_label: "Archive:",
        history_ver: "Ver",
        tab_siblings: "Direct Siblings",
        tab_relatives: "Relatives & Cousins",
        tab_solitary: "Solitary (No Relatives)",
        all_grades: "All Grades",
        relatives_tab_desc: "The Relatives section only displays students connected as cousins/relatives across different parent branches (excluding direct siblings).",
        solitary_tab_desc: "This section displays solitary students who have zero direct siblings and zero cousins/relatives in the school.",
        placeholder_search: "Quick search...",
        results_default_desc: "Click '☰' or 'Write' to enter student list, then click detect.",
        results_empty_title: "No groups found",
        results_empty_desc: "Please make sure full names are entered or try another matching method.",
        badge_groups_sib: "Sibling Families",
        badge_groups_rel: "Relative Groups",
        badge_groups_sol: "Solitary Students",
        family_label: "Shared Grandfather / Family:",
        maternal_family_label: "Shared Mother:",
        siblings_count: "Students:",
        grade_label: "Grade:",
        section_label: "Section:",
        mother_label: "Mother:",
        footer_text: "🔒 All operations are processed securely offline on your device to ensure student privacy.",
        stat_total: "Total Students",
        stat_siblings: "Direct Siblings",
        stat_relatives: "Relatives",
        stat_twins: "Twins",
        stat_solitary: "Solitary (No Relatives)",
        dark_mode_dark: "Dark",
        dark_mode_light: "Light",
        twin_warn: "⚠️ Twin?",
        father_branch: "Father's Branch: ",
        branch_label: "Father's Branch / Parent:",
        students_suffix: "students",
        groups_suffix: "groups",
        copy_btn: "📋 Copy",
        copy_success: "Card copied successfully!",
        singletons_title: "List of Solitary Students (Zero Siblings & Zero Relatives):",
        no_singletons: "No solitary students found",
        singletons_count_suffix: "Solitary Students",
        csv_paternal_type: "Via Father (3rd & 4th Grandfather)",
        csv_maternal_type: "Via Mother",
        csv_solitary_type: "Solitary (No Siblings & No Relatives)",
        csv_type: "Relationship Type",
        csv_shared: "Shared Grandfather / Mother",
        csv_branch: "Branch / Father",
        csv_name: "Student Name",
        paternal_card_title: "Paternal Relatives (3rd & 4th Grandfather):",
        maternal_card_title: "Maternal Relatives (Shared Mother):",
        btn_install_app: "Install App",
        dir: "ltr"
    }
};

let currentLang = 'ku_ba';
let studentPool = []; 
let siblingResults = [];
let relativeResults = [];
let solitaryResults = [];
let activeTab = 'siblings';
let isSidebarClosed = false;

// ذاكرة تخزين مؤقت لتسريع تسوية النصوص والبحث
const normCache = new Map();
const prefixCache = new Map();

function showToast(msg) {
    const toast = document.getElementById('toast');
    const toastMsg = document.getElementById('toastMsg');
    if (toast && toastMsg) {
        toastMsg.textContent = msg;
        toast.classList.add('show');
        setTimeout(() => toast.classList.remove('show'), 2000);
    }
}

function toggleDropdown(id) {
    const el = document.getElementById(id);
    if (el) el.classList.toggle('hidden');
}

// فتح وإغلاق لوحة الإدخال الجانبية
function toggleSidebarInPlace() {
    const body = document.body;
    const reopenBtn = document.getElementById('reopenSidebarBtn');
    isSidebarClosed = !isSidebarClosed;

    if (isSidebarClosed) {
        body.classList.add('sidebar-is-closed');
        if (reopenBtn) {
            reopenBtn.classList.remove('hidden');
            reopenBtn.classList.add('flex');
        }
    } else {
        body.classList.remove('sidebar-is-closed');
        if (reopenBtn) {
            reopenBtn.classList.add('hidden');
            reopenBtn.classList.remove('flex');
        }
    }
}

// التبديل السريع للشاشات الصغيرة
function setMobileView(view) {
    const sidebar = document.getElementById('sidebarCol');
    const results = document.getElementById('resultsCol');
    const btnInput = document.getElementById('btn-show-input');
    const btnResults = document.getElementById('btn-show-results');

    if (window.innerWidth >= 1024) return;

    if (view === 'input') {
        if (sidebar) sidebar.style.display = 'flex';
        if (results) results.style.display = 'none';
        if (btnInput) btnInput.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn text-white flex items-center justify-center gap-1 shadow-xs";
        if (btnResults) btnResults.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn-sec flex items-center justify-center gap-1";
    } else {
        if (sidebar) sidebar.style.display = 'none';
        if (results) results.style.display = 'flex';
        if (btnInput) btnInput.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn-sec flex items-center justify-center gap-1";
        if (btnResults) btnResults.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn text-white flex items-center justify-center gap-1 shadow-xs";
    }
}

function triggerMobileResults() {
    if (window.innerWidth < 1024) {
        setMobileView('results');
    }
}

function openDataModal(tabMode = 'paste') {
    const modal = document.getElementById('dataInputModal');
    if (modal) {
        modal.classList.remove('hidden');
        switchModalTab(tabMode);
    }
}

function closeDataModal() {
    const modal = document.getElementById('dataInputModal');
    if (modal) modal.classList.add('hidden');
}

function handleModalBackdropClick(e) {
    if (e.target.id === 'dataInputModal') closeDataModal();
}

function switchModalTab(mode) {
    const uploadSec = document.getElementById('modalUploadSection');
    const pasteSec = document.getElementById('modalPasteSection');
    const uploadBtn = document.getElementById('modal-tab-upload-btn');
    const pasteBtn = document.getElementById('modal-tab-paste-btn');

    if (mode === 'upload') {
        uploadSec?.classList.remove('hidden');
        pasteSec?.classList.add('hidden');
        if (uploadBtn) uploadBtn.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn text-white flex items-center justify-center gap-1 shadow-xs";
        if (pasteBtn) pasteBtn.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn-sec flex items-center justify-center gap-1";
    } else {
        uploadSec?.classList.add('hidden');
        pasteSec?.classList.remove('hidden');
        if (uploadBtn) uploadBtn.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn-sec flex items-center justify-center gap-1";
        if (pasteBtn) pasteBtn.className = "flex-1 py-1.5 px-2.5 rounded-xl text-xs font-bold transition clay-btn text-white flex items-center justify-center gap-1 shadow-xs";
        
        requestAnimationFrame(() => {
            document.getElementById('namesInput')?.focus();
        });
    }
}

function saveModalAndDetect() {
    closeDataModal();
    processNames();
    showToast(translations[currentLang].modal_save_detect);
}

document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeDataModal();
});

function selectLang(lang) {
    if (currentLang === lang) {
        toggleDropdown('langMenu');
        return;
    }
    currentLang = lang;
    const labelEl = document.getElementById('currentLangLabel');
    if (labelEl) {
        labelEl.textContent = {
            'ku_ba': 'بادینی',
            'ku_so': 'سۆرانی',
            'ar': 'العربية',
            'en': 'English'
        }[lang];
    }
    toggleDropdown('langMenu');
    changeLanguage(lang);
}

// تبديل لغات فوري فائق السرعة
function changeLanguage(lang) {
    const dict = translations[lang];
    document.documentElement.dir = dict.dir;
    document.documentElement.lang = (lang === 'ku_ba' || lang === 'ku_so') ? 'ku' : lang;

    const idsMap = {
        'ui-title': dict.title,
        'ui-subtitle': dict.subtitle,
        'ui-input-label': dict.input_label,
        'ui-top-sidebar-btn': dict.top_sidebar_btn,
        'ui-mob-sidebar-toggle': dict.mob_sidebar_toggle,
        'ui-mob-paste-tab': dict.mob_paste_tab,
        'ui-data-status-title': dict.data_status_title,
        'ui-btn-open-editor': dict.btn_open_editor,
        'ui-btn-upload-file': dict.btn_upload_file,
        'ui-modal-title': dict.modal_title,
        'ui-modal-tab-upload': dict.modal_tab_upload,
        'ui-modal-tab-paste': dict.modal_tab_paste,
        'ui-modal-save-detect': dict.modal_save_detect,
        'ui-modal-close': dict.modal_close,
        'ui-modal-clear': dict.modal_clear,
        'ui-instructions-title': dict.instructions_title,
        'ui-instruction-1': dict.instruction_1,
        'ui-example-1': dict.example_1,
        'ui-sep-label': dict.sep_label,
        'ui-match-label': dict.match_label,
        'ui-relatives-mode-label': dict.relatives_mode_label,
        'ui-relatives-badge': dict.relatives_badge,
        'ui-grandpa-level-label': dict.grandpa_level_label,
        'ui-prefix-label': dict.prefix_label,
        'ui-sort-default': dict.sort_default,
        'ui-sort-alpha': dict.sort_alpha,
        'ui-btn-detect-text': dict.btn_detect,
        'ui-btn-excel': dict.btn_excel,
        'ui-btn-singletons-text': dict.btn_singletons,
        'ui-btn-print': dict.btn_print,
        'ui-btn-clear': dict.btn_clear,
        'ui-history-label': dict.history_label,
        'ui-relatives-tab-desc': dict.relatives_tab_desc,
        'ui-solitary-tab-desc': dict.solitary_tab_desc,
        'ui-footer-text': dict.footer_text,
        'ui-all-grades': dict.all_grades,
        'opt-sep-auto': dict.sep_auto,
        'opt-sep-tab': dict.sep_tab,
        'opt-sep-comma': dict.sep_comma,
        'opt-sep-semicolon': dict.sep_semicolon,
        'opt-match-rest': dict.match_rest,
        'opt-match-last2': dict.match_last2,
        'opt-match-family': dict.match_family,
        'opt-rel-paternal': dict.paternal_mode,
        'opt-rel-maternal': dict.maternal_mode,
        'opt-rel-both': dict.both_mode,
        'opt-grandpa-34': dict.grandpa_3_4,
        'opt-grandpa-3': dict.grandpa_3,
        'opt-grandpa-4': dict.grandpa_4,
        'opt-grandpa-all': dict.all_ancestors,
        'tab-siblings-label': dict.tab_siblings,
        'tab-relatives-label': dict.tab_relatives,
        'tab-solitary-label': dict.tab_solitary,
        'stat-total-title': dict.stat_total,
        'stat-siblings-title': dict.stat_siblings,
        'stat-relatives-title': dict.stat_relatives,
        'stat-twins-title': dict.stat_twins,
        'stat-solitary-label': dict.stat_solitary,
        'ui-btn-install-app': dict.btn_install_app || "دانانا بەرنامەی"
    };

    for (const [id, text] of Object.entries(idsMap)) {
        const el = document.getElementById(id);
        if (el) el.textContent = text;
    }

    const isDark = document.documentElement.classList.contains('theme-dark');
    const darkBtn = document.getElementById('darkBtnText');
    if (darkBtn) darkBtn.textContent = isDark ? dict.dark_mode_light : dict.dark_mode_dark;

    const inputEl = document.getElementById('namesInput');
    if (inputEl) inputEl.placeholder = dict.placeholder_input;
    const searchEl = document.getElementById('searchFilter');
    if (searchEl) searchEl.placeholder = dict.placeholder_search;

    onInputChanged();
    renderHistoryUI();

    if (studentPool.length > 0) {
        displayResults();
    } else {
        clearResultsArea();
    }
}

let inputDebounceTimer = null;
function onInputChanged() {
    clearTimeout(inputDebounceTimer);
    inputDebounceTimer = setTimeout(() => {
        saveBackupLocally();
        const val = document.getElementById('namesInput').value;
        const lines = val.split('\n').filter(l => l.trim().length > 0).length;
        const lineSuffix = currentLang === 'en' ? 'lines' : 'دێڕ';

        const modalCounter = document.getElementById('modalLineCounter');
        if (modalCounter) modalCounter.textContent = `${lines} ${lineSuffix}`;

        const sideBadge = document.getElementById('sideDataBadge');
        if (sideBadge) sideBadge.textContent = `${lines} ${lineSuffix}`;
    }, 50);
}

function handleFileInput(e) {
    const file = e.target.files[0];
    if (file) parseUploadedFile(file);
    e.target.value = '';
}

function handleFileDrop(e) {
    e.preventDefault();
    document.getElementById('dropZone')?.classList.remove('drag-over');
    const file = e.dataTransfer.files[0];
    if (file) parseUploadedFile(file);
}

function parseUploadedFile(file) {
    const fileName = file.name.toLowerCase();
    const isExcel = fileName.endsWith('.xlsx') || fileName.endsWith('.xls') || fileName.endsWith('.xlsm') || fileName.endsWith('.xlsb');
    
    if (isExcel && typeof XLSX !== 'undefined') {
        const reader = new FileReader();
        reader.onload = function(e) {
            try {
                const data = new Uint8Array(e.target.result);
                const workbook = XLSX.read(data, { type: 'array' });
                let allLines = [];

                for (let sIdx = 0; sIdx < workbook.SheetNames.length; sIdx++) {
                    const sheetName = workbook.SheetNames[sIdx];
                    const worksheet = workbook.Sheets[sheetName];
                    if (!worksheet) continue;

                    const rawRows = XLSX.utils.sheet_to_json(worksheet, { header: 1, defval: '' });
                    if (rawRows && rawRows.length > 0) {
                        const lines = extractTargetColumnsFromMatrix(rawRows);
                        if (lines.length > 0) {
                            allLines = lines;
                            break;
                        }
                    }
                }

                if (allLines.length === 0 && workbook.SheetNames.length > 0) {
                    const worksheet = workbook.Sheets[workbook.SheetNames[0]];
                    const rawRows = XLSX.utils.sheet_to_json(worksheet, { header: 1, defval: '' });
                    if (rawRows && rawRows.length > 0) {
                        rawRows.forEach(row => {
                            const cells = row.map(c => String(c !== null && c !== undefined ? c : '').trim()).filter(c => c !== '');
                            if (cells.length > 0) allLines.push(cells.join('\t'));
                        });
                    }
                }

                if (allLines.length > 0) {
                    document.getElementById('namesInput').value = allLines.join('\n');
                    onInputChanged();
                    closeDataModal();
                    processNames();
                    showToast(currentLang === 'en' ? 'Excel loaded!' : 'مەلەفێ إکسل ب سەرکەفتی هاتە وەرگرتن!');
                } else {
                    alert(currentLang === 'en' ? 'The uploaded Excel file contains no data!' : 'ملف الإكسل المرفوع لا يحتوي على بيانات!');
                }
            } catch (err) {
                console.error("Excel parse error:", err);
                alert(currentLang === 'en' ? 'Error reading Excel file.' : 'حدث خطأ أثناء قراءة ملف الإكسل.');
            }
        };
        reader.readAsArrayBuffer(file);
    } else {
        const reader = new FileReader();
        reader.onload = function(evt) {
            const text = evt.target.result;
            const rawLines = text.split(/\r?\n/).filter(l => l.trim().length > 0);
            if (rawLines.length > 0) {
                const firstLine = rawLines[0];
                let sep = '\t';
                if (firstLine.includes('\t')) sep = '\t';
                else if (firstLine.includes(',')) sep = ',';
                else if (firstLine.includes(';')) sep = ';';

                const matrix = rawLines.map(l => l.split(sep));
                const lines = extractTargetColumnsFromMatrix(matrix);
                document.getElementById('namesInput').value = lines.length > 0 ? lines.join('\n') : text;
            } else {
                document.getElementById('namesInput').value = text;
            }
            onInputChanged();
            closeDataModal();
            processNames();
            showToast(currentLang === 'en' ? 'Data imported!' : 'داتا ب سەرکەفتی هاتە وەرگرتن!');
        };
        reader.readAsText(file, 'UTF-8');
    }
}

function extractTargetColumnsFromMatrix(matrix) {
    if (!matrix || matrix.length === 0) return [];

    let headerRowIdx = -1;
    let nameCol = -1;
    let motherCol = -1;
    let gradeCol = -1;
    let sectionCol = -1;

    const isSchoolOrgWord = (txt) => {
        const lower = txt.toLowerCase();
        return lower.includes('قوتابخان') || lower.includes('مدرسة') || lower.includes('پەروەرد') || lower.includes('تربية') || lower.includes('مديرية') || lower.includes('حکومی') || lower.includes('لۆکاڵ') || lower.includes('حكومي');
    };

    for (let i = 0; i < Math.min(15, matrix.length); i++) {
        const row = matrix[i];
        if (!row || !Array.isArray(row)) continue;

        row.forEach((cellVal, colIdx) => {
            if (cellVal === null || cellVal === undefined) return;
            const str = String(cellVal).trim().toLowerCase();
            if (!str) return;

            if (str.includes('دەیک') || str.includes('دایک') || str.includes('ام') || str.includes('أم') || str.includes('mother') || str.includes('والدة')) {
                motherCol = colIdx;
                headerRowIdx = i;
            } else if ((str.includes('ناڤ') || str.includes('ناو') || str.includes('طالب') || str.includes('تلميذ') || str.includes('student') || str.includes('قوتابی') || str.includes('رباعي') || str.includes('ثلاثي') || str.includes('سیانی') || str.includes('چواری') || str.includes('تەواو') || str === 'الاسم' || str === 'اسم' || str === 'name') && !isSchoolOrgWord(str)) {
                if (!str.includes('دەیک') && !str.includes('دایک') && !str.includes('ام') && !str.includes('أم') && !str.includes('mother')) {
                    nameCol = colIdx;
                    headerRowIdx = i;
                }
            } else if (str.includes('پۆل') || str.includes('قۆناغ') || str.includes('صف') || str.includes('مرحلة') || str.includes('grade') || str.includes('class') || str.includes('stage')) {
                gradeCol = colIdx;
                headerRowIdx = i;
            } else if (str.includes('شعب') || str.includes('بەش') || str.includes('قسم') || str.includes('فرع') || str.includes('section') || str.includes('group')) {
                sectionCol = colIdx;
                headerRowIdx = i;
            }
        });

        if (nameCol !== -1) {
            headerRowIdx = i;
            break;
        }
    }

    const startRow = headerRowIdx !== -1 ? headerRowIdx + 1 : 0;
    const extractedLines = [];

    if (nameCol === -1) {
        const sampleRows = matrix.slice(startRow, startRow + 25);
        const colStats = {};

        sampleRows.forEach(row => {
            row.forEach((cell, idx) => {
                const s = String(cell || '').trim();
                if (!colStats[idx]) colStats[idx] = { wordCounts: [], isNames: 0, isOrg: 0 };
                if (s.length > 0) {
                    const words = s.split(/\s+/);
                    colStats[idx].wordCounts.push(words.length);
                    if (words.length >= 3 && !isSchoolOrgWord(s) && isNaN(s)) colStats[idx].isNames++;
                    if (isSchoolOrgWord(s)) colStats[idx].isOrg++;
                }
            });
        });

        let bestCol = -1;
        let bestScore = -1;
        for (const idx in colStats) {
            const st = colStats[idx];
            if (st.isOrg === 0 && st.isNames > bestScore) {
                bestScore = st.isNames;
                bestCol = parseInt(idx);
            }
        }
        if (bestCol !== -1) nameCol = bestCol;
    }

    for (let r = startRow; r < matrix.length; r++) {
        const row = matrix[r];
        if (!row || !Array.isArray(row)) continue;

        let studentName = '';
        let motherName = '';
        let grade = '';
        let section = '';

        if (nameCol !== -1) {
            studentName = String(row[nameCol] || '').trim();
            if (motherCol !== -1) motherName = String(row[motherCol] || '').trim();
            if (gradeCol !== -1) grade = String(row[gradeCol] || '').trim();
            if (sectionCol !== -1) section = String(row[sectionCol] || '').trim();
        } else {
            const nonEmpties = row.map(c => String(c || '').trim()).filter(c => c !== '');
            if (nonEmpties.length >= 4) {
                studentName = nonEmpties[0];
                motherName = nonEmpties[1];
                grade = nonEmpties[2];
                section = nonEmpties[3];
            } else if (nonEmpties.length === 3) {
                studentName = nonEmpties[0];
                grade = nonEmpties[1];
                section = nonEmpties[2];
            } else if (nonEmpties.length > 0) {
                studentName = nonEmpties[0];
                if (nonEmpties.length > 1) grade = nonEmpties[1];
                if (nonEmpties.length > 2) section = nonEmpties[2];
            }
        }

        if (studentName && studentName.length > 1 && !isSchoolOrgWord(studentName)) {
            let line = studentName;
            if (motherName && motherName.length > 1 && !isSchoolOrgWord(motherName) && isNaN(motherName)) {
                line += `\t${motherName}\t${grade || '-'}\t${section || '-'}`;
            } else {
                line += `\t${grade || '-'}\t${section || '-'}`;
            }
            extractedLines.push(line);
        }
    }

    return extractedLines;
}

// تسريع تجريد السوابق باستخدام الذاكرة
function stripPrefixes(text) {
    if (!document.getElementById('stripPrefixOpt').checked || !text) return text || '';
    if (prefixCache.has(text)) return prefixCache.get(text);

    const res = text.trim()
        .replace(/^(ال|أبو|ابو|أم|ام|ابن|بن|ملا|خان|شيخ|كاكا)\s+/g, '')
        .replace(/\s+(ال|أبو|ابو|أم|ام|ابن|بن|شيخ)\s+/g, ' ');
    prefixCache.set(text, res);
    return res;
}

// تسريع وتوحيد النصوص (Memoized Normalization)
function normalizeText(text) {
    if (!text) return '';
    if (normCache.has(text)) return normCache.get(text);

    const res = text.trim()
        .replace(/[أإآ]/g, 'ا')
        .replace(/ة/g, 'ه')
        .replace(/ى/g, 'ي')
        .replace(/[\u064B-\u065F]/g, '')
        .replace(/\s+/g, ' ');
    normCache.set(text, res);
    return res;
}

function processNames() {
    const rawInput = document.getElementById('namesInput').value;
    const lines = rawInput.split('\n').map(line => line.trim()).filter(line => line.length > 0);
    
    if (lines.length === 0) {
        clearResultsArea();
        return;
    }

    studentPool = [];
    const sepMode = document.getElementById('dataSeparator').value;

    lines.forEach((line, index) => {
        let parts = [];
        let sep = ',';
        if (sepMode === 'auto') {
            if (line.includes('\t')) sep = '\t';
            else if (line.includes('،')) sep = '،';
            else if (line.includes(';')) sep = ';';
        } else {
            sep = sepMode === 'tab' ? '\t' : (sepMode === 'comma' ? ',' : ';');
        }

        parts = line.split(sep).map(p => p.trim());
        
        let name = parts[0] || '';
        let mother = '';
        let grade = currentLang === 'en' ? 'N/A' : (currentLang === 'ar' ? 'غير محدد' : 'دیارنەکری');
        let section = currentLang === 'en' ? 'N/A' : (currentLang === 'ar' ? 'غير محدد' : 'دیارنەکری');
        let guardian = '';

        if (parts.length >= 4) {
            mother = parts[1] || '';
            grade = parts[2] || grade;
            section = parts[3] || section;
            guardian = parts[4] || '';
        } else if (parts.length === 3) {
            grade = parts[1] || grade;
            section = parts[2] || section;
        } else if (parts.length === 2) {
            grade = parts[1] || grade;
        }

        if (name) {
            studentPool.push({ id: index, name, grade, section, mother, guardian });
        }
    });

    const relMode = document.getElementById('relativesSearchMode').value;
    const grandpaBox = document.getElementById('grandpaMatchBox');
    if (grandpaBox) {
        grandpaBox.style.display = (relMode === 'mother') ? 'none' : 'block';
    }

    renderEngine();
    updateHistoryArchive(rawInput);
}

function extractGrandfatherKey(name, depthMode, isStrip) {
    const normalized = normalizeText(isStrip ? stripPrefixes(name) : name);
    const words = normalized.split(' ').filter(w => w.length > 0);

    if (words.length < 2) {
        return { father: words[0] || '', grandpaKey: '' };
    }

    const father = words[1];

    let grandpaKey = '';
    if (depthMode === 'grandpa-3') {
        grandpaKey = words[2] || words[1];
    } else if (depthMode === 'grandpa-4') {
        grandpaKey = words[3] || words[2] || words[1];
    } else if (depthMode === 'all-ancestors') {
        grandpaKey = words.length > 2 ? words.slice(2).join(' ') : words[1];
    } else {
        if (words.length >= 4) {
            grandpaKey = words[2] + ' ' + words[3];
        } else if (words.length === 3) {
            grandpaKey = words[2];
        } else {
            grandpaKey = words[1];
        }
    }

    return { father, grandpaKey };
}

function renderEngine() {
    const matchType = document.getElementById('matchType').value;
    const isStrip = document.getElementById('stripPrefixOpt').checked;
    const relSearchMode = document.getElementById('relativesSearchMode').value;
    const grandpaDepth = document.getElementById('grandpaMatchDepth').value;
    
    // 1. كشف الإخوة الأشقاء
    const siblingGroups = {};
    let twinCount = 0;

    studentPool.forEach(student => {
        let key = '';
        if (student.guardian) {
            key = normalizeText(student.guardian);
        } else {
            const normalized = normalizeText(isStrip ? stripPrefixes(student.name) : student.name);
            const words = normalized.split(' ').filter(w => w.length > 0);
            
            if (matchType === 'all-except-first' && words.length > 1) {
                key = words.slice(1).join(' ');
            } else if (matchType === 'last-two' && words.length >= 2) {
                key = words.slice(-2).join(' ');
            } else if (matchType === 'last-one' && words.length >= 1) {
                key = words.slice(-1).join(' ');
            } else if (words.length > 1) {
                key = words.slice(1).join(' ');
            }
        }

        if (key) {
            if (!siblingGroups[key]) siblingGroups[key] = [];
            siblingGroups[key].push(student);
        }
    });

    siblingResults = [];
    let linkedSiblingsCount = 0;
    const siblingStudentIds = new Set();

    for (const key in siblingGroups) {
        if (siblingGroups[key].length > 1) {
            siblingResults.push({ sharedName: key, students: siblingGroups[key] });
            linkedSiblingsCount += siblingGroups[key].length;
            siblingGroups[key].forEach(s => siblingStudentIds.add(s.id));

            const gradesInGroup = siblingGroups[key].map(s => s.grade);
            const duplicateGrades = gradesInGroup.filter((item, index) => gradesInGroup.indexOf(item) !== index);
            if (duplicateGrades.length > 0) twinCount += duplicateGrades.length + 1;
        }
    }

    // 2. كشف الأقارب
    relativeResults = [];
    let totalRelativesMatched = 0;
    const relativeStudentIds = new Set();

    if (relSearchMode === 'father-grandpa' || relSearchMode === 'both') {
        const paternalRelativesMap = {};

        studentPool.forEach(student => {
            const { father, grandpaKey } = extractGrandfatherKey(student.name, grandpaDepth, isStrip);
            
            if (grandpaKey && grandpaKey.length > 1) {
                if (!paternalRelativesMap[grandpaKey]) paternalRelativesMap[grandpaKey] = {};
                if (!paternalRelativesMap[grandpaKey][father]) paternalRelativesMap[grandpaKey][father] = [];
                paternalRelativesMap[grandpaKey][father].push(student);
            }
        });

        for (const grandpaKey in paternalRelativesMap) {
            const fatherBranches = Object.keys(paternalRelativesMap[grandpaKey]);
            
            if (fatherBranches.length > 1) {
                const branches = {};
                let groupCount = 0;
                fatherBranches.forEach(father => {
                    branches[father] = paternalRelativesMap[grandpaKey][father];
                    groupCount += paternalRelativesMap[grandpaKey][father].length;
                    paternalRelativesMap[grandpaKey][father].forEach(s => relativeStudentIds.add(s.id));
                });

                totalRelativesMatched += groupCount;
                relativeResults.push({
                    type: 'paternal',
                    sharedKey: grandpaKey,
                    branches: branches,
                    totalCount: groupCount
                });
            }
        }
    }

    if (relSearchMode === 'mother' || relSearchMode === 'both') {
        const maternalRelativesMap = {};

        studentPool.forEach(student => {
            if (student.mother && student.mother.trim().length > 1) {
                const motherKey = normalizeText(isStrip ? stripPrefixes(student.mother) : student.mother);
                const normalizedStudentName = normalizeText(student.name);
                const words = normalizedStudentName.split(' ');
                const fatherOrBranch = words.length > 1 ? words.slice(1).join(' ') : student.name;

                if (!maternalRelativesMap[motherKey]) maternalRelativesMap[motherKey] = {};
                if (!maternalRelativesMap[motherKey][fatherOrBranch]) maternalRelativesMap[motherKey][fatherOrBranch] = [];
                maternalRelativesMap[motherKey][fatherOrBranch].push(student);
            }
        });

        for (const motherKey in maternalRelativesMap) {
            const branches = Object.keys(maternalRelativesMap[motherKey]);
            const totalMaternalStudents = Object.values(maternalRelativesMap[motherKey]).reduce((acc, curr) => acc + curr.length, 0);

            if (totalMaternalStudents > 1) {
                const branchData = {};
                branches.forEach(b => {
                    branchData[b] = maternalRelativesMap[motherKey][b];
                    maternalRelativesMap[motherKey][b].forEach(s => relativeStudentIds.add(s.id));
                });

                if (relSearchMode === 'mother' || branches.length > 1) {
                    totalRelativesMatched += totalMaternalStudents;
                    relativeResults.push({
                        type: 'maternal',
                        sharedKey: motherKey,
                        branches: branchData,
                        totalCount: totalMaternalStudents
                    });
                }
            }
        }
    }

    // 3. كشف الأشخاص الوحيدين
    solitaryResults = studentPool.filter(student => !siblingStudentIds.has(student.id) && !relativeStudentIds.has(student.id));

    // ترتيب الفرز
    const sortMode = document.getElementById('resultsSort').value;
    if (sortMode === 'count-desc') {
        siblingResults.sort((a, b) => b.students.length - a.students.length);
        relativeResults.sort((a, b) => b.totalCount - a.totalCount);
        solitaryResults.sort((a, b) => a.name.localeCompare(b.name));
    } else {
        siblingResults.sort((a, b) => a.sharedName.localeCompare(b.sharedName));
        relativeResults.sort((a, b) => a.sharedKey.localeCompare(b.sharedKey));
        solitaryResults.sort((a, b) => a.name.localeCompare(b.name));
    }

    // تحديث الإحصائيات
    document.getElementById('stat-total').textContent = studentPool.length;
    document.getElementById('stat-siblings').textContent = linkedSiblingsCount;
    document.getElementById('stat-relatives').textContent = totalRelativesMatched;
    document.getElementById('stat-twins').textContent = twinCount;
    document.getElementById('stat-solitary').textContent = solitaryResults.length;

    const mobBadge = document.getElementById('mobResultsBadge');
    if (mobBadge) {
        const totalFound = linkedSiblingsCount + totalRelativesMatched + solitaryResults.length;
        mobBadge.textContent = totalFound;
        if (totalFound > 0) mobBadge.classList.remove('hidden');
        else mobBadge.classList.add('hidden');
    }

    populateGradeFilter();
    displayResults();
}

function switchResultTab(tab) {
    activeTab = tab;
    const sibBtn = document.getElementById('tab-siblings-btn');
    const relBtn = document.getElementById('tab-relatives-btn');
    const solBtn = document.getElementById('tab-solitary-btn');
    const relNotice = document.getElementById('relativesTabNotice');
    const solNotice = document.getElementById('solitaryTabNotice');

    sibBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn-sec flex items-center gap-1";
    relBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn-sec flex items-center gap-1";
    solBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn-sec flex items-center gap-1 text-rose-700 dark:text-rose-300";

    if (relNotice) relNotice.classList.add('hidden');
    if (solNotice) solNotice.classList.add('hidden');

    if (tab === 'siblings') {
        sibBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn text-white flex items-center gap-1";
    } else if (tab === 'relatives') {
        relBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn text-white flex items-center gap-1";
        if (relNotice) relNotice.classList.remove('hidden');
    } else if (tab === 'solitary') {
        solBtn.className = "px-2.5 py-1.5 sm:px-3 sm:py-1.5 rounded-xl text-[11px] font-bold transition-all clay-btn text-white bg-rose-500/90 dark:bg-rose-600 flex items-center gap-1";
        if (solNotice) solNotice.classList.remove('hidden');
    }

    displayResults();
}

function populateGradeFilter() {
    const filterSelect = document.getElementById('gradeFilter');
    const currentVal = filterSelect.value;
    filterSelect.innerHTML = `<option id="ui-all-grades" value="all">${translations[currentLang].all_grades}</option>`;
    
    const gradesSet = new Set();
    studentPool.forEach(s => { if (s.grade) gradesSet.add(s.grade); });
    
    gradesSet.forEach(grade => {
        const opt = document.createElement('option');
        opt.value = grade;
        opt.textContent = grade;
        filterSelect.appendChild(opt);
    });
    
    filterSelect.value = currentVal;
    filterSelect.classList.remove('hidden');
}

function displayResults() {
    const resultsArea = document.getElementById('resultsArea');
    const resultsCount = document.getElementById('resultsCount');
    const searchFilterContainer = document.getElementById('searchFilterContainer');
    const dict = translations[currentLang];

    let currentResults = [];
    let badgeLabel = '';

    if (activeTab === 'siblings') {
        currentResults = siblingResults;
        badgeLabel = dict.badge_groups_sib;
    } else if (activeTab === 'relatives') {
        currentResults = relativeResults;
        badgeLabel = dict.badge_groups_rel;
    } else if (activeTab === 'solitary') {
        currentResults = solitaryResults;
        badgeLabel = dict.badge_groups_sol;
    }

    if (currentResults.length === 0) {
        resultsArea.className = "flex-grow flex flex-col justify-center items-center text-slate-400 py-10 text-center";
        resultsArea.innerHTML = `
            <div class="w-14 h-14 rounded-full bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-xl mb-2.5 text-slate-400">
                🔍
            </div>
            <p class="font-bold text-slate-700 dark:text-slate-200 text-xs sm:text-sm">${activeTab === 'solitary' ? dict.no_singletons : dict.results_empty_title}</p>
            <p class="text-[11px] mt-1 text-slate-500 max-w-sm">${dict.results_empty_desc}</p>`;
        resultsCount.classList.add('hidden');
        searchFilterContainer.classList.add('hidden');
        return;
    }

    resultsCount.textContent = `${currentResults.length} ${badgeLabel}`;
    resultsCount.classList.remove('hidden');
    searchFilterContainer.classList.remove('hidden');

    resultsArea.className = "flex-grow space-y-3 overflow-y-auto max-h-[580px] custom-scroll pl-1 pr-1 py-1";

    if (activeTab === 'siblings') {
        renderSiblingsList(siblingResults);
    } else if (activeTab === 'relatives') {
        renderRelativesList(relativeResults);
    } else if (activeTab === 'solitary') {
        renderSolitaryList(solitaryResults);
    }
}

function renderSiblingsList(groups) {
    const resultsArea = document.getElementById('resultsArea');
    const dict = translations[currentLang];
    let htmlContent = '';

    groups.forEach((group, groupIdx) => {
        htmlContent += `
            <div class="group-card clay-card p-3 sm:p-3.5 transition duration-150 hover:scale-[1.005]" 
                 data-shared="${group.sharedName}" 
                 data-search-pool="${group.students.map(s => `${s.name} ${s.grade} ${s.section} ${s.mother || ''}`).join(' ')}">
                
                <div class="flex flex-wrap justify-between items-center mb-2.5 pb-1.5 border-b border-slate-100 dark:border-slate-800 gap-1.5">
                    <span class="text-xs font-extrabold text-blue-900/90 dark:text-blue-200 bg-blue-50/80 dark:bg-slate-800 px-3 py-0.5 rounded-full shadow-xs">
                        👨‍👩‍👦 ${dict.family_label} ${group.sharedName}
                    </span>
                    <div class="flex items-center gap-1.5">
                        <button onclick="copyFamilyCard(${groupIdx})" class="clay-btn-sec text-[9px] px-2 py-0.5 font-bold no-print">${dict.copy_btn}</button>
                        <span class="clay-badge text-[9px] text-emerald-700 dark:text-emerald-300 px-2 py-0.5 font-bold">
                            ${dict.siblings_count} ${group.students.length}
                        </span>
                    </div>
                </div>
                
                <ul class="space-y-1.5">
                    ${group.students.map((student) => {
                        const isTwin = group.students.filter(s => s.grade === student.grade).length > 1;
                        return `
                            <li class="flex flex-wrap items-center justify-between text-xs p-2 bg-slate-50/90 dark:bg-slate-800/50 rounded-xl hover:bg-white dark:hover:bg-slate-800 transition shadow-xs border border-transparent hover:border-slate-100 gap-1.5">
                                <div class="flex flex-wrap items-center gap-1.5">
                                    <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'name', this.innerText)" class="font-bold text-slate-800 dark:text-slate-100 focus:outline-none focus:bg-amber-50 dark:focus:bg-slate-700 rounded px-1">${student.name}</span>
                                    ${isTwin ? `<span class="bg-amber-100/80 text-amber-800 dark:bg-amber-950 dark:text-amber-300 text-[8px] px-1.5 py-0.2 rounded-full font-bold">${dict.twin_warn}</span>` : ''}
                                    ${student.mother ? `<span class="bg-pink-50/80 text-pink-700 dark:bg-pink-950 dark:text-pink-300 text-[8px] px-1.5 py-0.2 rounded-full font-semibold border border-pink-200/50">🌸 ${dict.mother_label} ${student.mother}</span>` : ''}
                                </div>
                                <div class="flex items-center gap-1.5">
                                    <span class="text-[9px] text-slate-500">${dict.grade_label}</span>
                                    <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'grade', this.innerText)" class="bg-blue-50/80 dark:bg-slate-700 text-blue-700 dark:text-blue-300 px-1.5 py-0.2 rounded font-bold text-[10px]">${student.grade}</span>
                                    
                                    <span class="text-[9px] text-slate-500">${dict.section_label}</span>
                                    <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'section', this.innerText)" class="bg-emerald-50/80 dark:bg-slate-700 text-emerald-700 dark:text-emerald-300 px-1.5 py-0.2 rounded font-bold text-[10px]">${student.section}</span>
                                    
                                    <button onclick="removeStudent(${student.id})" class="text-red-500 hover:text-red-700 text-xs font-bold ml-1 no-print">×</button>
                                </div>
                            </li>
                        `;
                    }).join('')}
                </ul>
            </div>
        `;
    });
    resultsArea.innerHTML = htmlContent;
}

function renderRelativesList(groups) {
    const resultsArea = document.getElementById('resultsArea');
    const dict = translations[currentLang];
    let htmlContent = '';

    groups.forEach((group, groupIdx) => {
        const isMaternal = group.type === 'maternal';
        const headerColor = isMaternal ? 'border-pink-200 dark:border-pink-900 bg-pink-50/60 dark:bg-pink-950/30' : 'border-indigo-200 dark:border-indigo-900 bg-indigo-50/60 dark:bg-indigo-950/30';
        const tagColor = isMaternal ? 'text-pink-900/90 dark:text-pink-200' : 'text-indigo-900/90 dark:text-indigo-200';
        const icon = isMaternal ? '🌸' : '🌿';
        const relCategory = isMaternal ? dict.maternal_card_title : dict.paternal_card_title;

        htmlContent += `
            <div class="group-card clay-card p-3 sm:p-3.5 transition duration-150 hover:scale-[1.005]"
                 data-shared="${group.sharedKey}"
                 data-search-pool="${Object.values(group.branches).flatMap(b => b).map(s => `${s.name} ${s.grade} ${s.section} ${s.mother || ''}`).join(' ')}">
                
                <div class="flex flex-wrap justify-between items-center mb-2.5 pb-1.5 border-b ${headerColor} p-1.5 rounded-xl gap-1.5">
                    <div class="flex items-center gap-1.5">
                        <span class="text-sm">${icon}</span>
                        <div>
                            <span class="text-[9px] text-slate-500 font-bold block">${relCategory}</span>
                            <span class="text-xs font-extrabold ${tagColor}">
                                ${group.sharedKey}
                            </span>
                        </div>
                    </div>
                    <div class="flex items-center gap-1.5">
                        <button onclick="copyRelativeCard(${groupIdx})" class="clay-btn-sec text-[9px] px-2 py-0.5 font-bold shadow-xs no-print">${dict.copy_btn}</button>
                        <span class="clay-badge text-[9px] ${isMaternal ? 'text-pink-700 dark:text-pink-300' : 'text-indigo-700 dark:text-indigo-300'} px-2 py-0.5 font-bold">
                            ${dict.siblings_count} ${group.totalCount}
                        </span>
                    </div>
                </div>
                
                <div class="space-y-2">
                    ${Object.keys(group.branches).map(branchKey => {
                        const branchStudents = group.branches[branchKey];
                        return `
                            <div class="${isMaternal ? 'clay-card-maternal' : 'clay-card-blue'} p-2.5 rounded-xl">
                                <div class="text-[9px] font-bold text-slate-600 dark:text-slate-300 mb-1.5 flex items-center justify-between border-b border-slate-200/50 pb-0.5">
                                    <div class="flex items-center gap-1">
                                        <span>${isMaternal ? '🌸 ' + dict.branch_label : '🌿 ' + dict.father_branch}</span>
                                        <span class="font-extrabold ${tagColor}">${branchKey}</span>
                                    </div>
                                    <span class="text-[8px] px-1.5 py-0.2 bg-white/70 dark:bg-slate-800/70 rounded-full font-semibold">
                                        (${branchStudents.length} ${dict.students_suffix})
                                    </span>
                                </div>
                                <ul class="space-y-1">
                                    ${branchStudents.map(student => `
                                        <li class="flex flex-wrap items-center justify-between text-xs p-1.5 bg-white/90 dark:bg-slate-800/90 rounded-lg border border-transparent shadow-xs gap-1">
                                            <div class="flex flex-wrap items-center gap-1.5">
                                                <span class="font-bold text-slate-800 dark:text-slate-100">${student.name}</span>
                                                ${student.mother ? `<span class="text-[8px] bg-pink-50/80 dark:bg-slate-700 text-pink-700 dark:text-pink-300 px-1 py-0.2 rounded font-bold">🌸 ${dict.mother_label} ${student.mother}</span>` : ''}
                                            </div>
                                            <div class="flex gap-1 text-[9px]">
                                                <span class="clay-badge text-blue-800 dark:text-blue-200 px-1.5 py-0.2 font-bold">${dict.grade_label} ${student.grade}</span>
                                                <span class="clay-badge text-emerald-800 dark:text-emerald-200 px-1.5 py-0.2 font-bold">${dict.section_label} ${student.section}</span>
                                            </div>
                                        </li>
                                    `).join('')}
                                </ul>
                            </div>
                        `;
                    }).join('')}
                </div>
            </div>
        `;
    });

    resultsArea.innerHTML = htmlContent;
}

function renderSolitaryList(students) {
    const resultsArea = document.getElementById('resultsArea');
    const dict = translations[currentLang];
    let htmlContent = `
        <div class="clay-card p-2.5 sm:p-3 mb-2.5 border-rose-200 dark:border-rose-900 bg-rose-50/40 dark:bg-slate-800 flex items-center justify-between">
            <div class="flex items-center gap-2">
                <span class="text-base">👤</span>
                <div>
                    <h3 class="font-extrabold text-xs sm:text-sm text-rose-900/90 dark:text-rose-200">${dict.singletons_title}</h3>
                    <p class="text-[9px] text-slate-500 dark:text-slate-400">${dict.solitary_tab_desc}</p>
                </div>
            </div>
            <button onclick="copySolitaryList()" class="clay-btn-sec text-[9px] px-2 py-1 font-bold no-print flex items-center gap-1">
                <span>📋</span> <span>${dict.copy_btn}</span>
            </button>
        </div>
    `;

    students.forEach((student) => {
        htmlContent += `
            <div class="group-card clay-card p-2.5 sm:p-3 transition duration-150 hover:scale-[1.005]" 
                 data-shared="${student.name}" 
                 data-search-pool="${student.name} ${student.grade} ${student.section} ${student.mother || ''}">
                
                <div class="flex flex-wrap items-center justify-between text-xs gap-1.5">
                    <div class="flex flex-wrap items-center gap-1.5">
                        <span class="w-5 h-5 rounded-full bg-rose-100/80 dark:bg-rose-950 text-rose-600 dark:text-rose-300 flex items-center justify-center font-bold text-[9px]">👤</span>
                        <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'name', this.innerText)" class="font-bold text-slate-800 dark:text-slate-100 focus:outline-none focus:bg-amber-50 dark:focus:bg-slate-700 rounded px-1">${student.name}</span>
                        ${student.mother ? `<span class="bg-pink-50/80 text-pink-700 dark:bg-pink-950 dark:text-pink-300 text-[8px] px-1.5 py-0.2 rounded-full font-semibold border border-pink-200/50">🌸 ${dict.mother_label} ${student.mother}</span>` : ''}
                        <span class="bg-rose-100/70 dark:bg-rose-950 text-rose-700 dark:text-rose-300 text-[8px] px-1.5 py-0.2 rounded-full font-bold">بێ کەسوکار</span>
                    </div>
                    <div class="flex items-center gap-1.5">
                        <span class="text-[9px] text-slate-500">${dict.grade_label}</span>
                        <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'grade', this.innerText)" class="bg-blue-50/80 dark:bg-slate-700 text-blue-700 dark:text-blue-300 px-1.5 py-0.2 rounded font-bold text-[10px]">${student.grade}</span>
                        
                        <span class="text-[9px] text-slate-500">${dict.section_label}</span>
                        <span contenteditable="true" onblur="updateStudentValue(${student.id}, 'section', this.innerText)" class="bg-emerald-50/80 dark:bg-slate-700 text-emerald-700 dark:text-emerald-300 px-1.5 py-0.2 rounded font-bold text-[10px]">${student.section}</span>
                        
                        <button onclick="removeStudent(${student.id})" class="text-red-500 hover:text-red-700 text-xs font-bold ml-1 no-print">×</button>
                    </div>
                </div>
            </div>
        `;
    });

    resultsArea.innerHTML = htmlContent;
}

function updateStudentValue(id, field, value) {
    const student = studentPool.find(s => s.id === id);
    if (student) {
        student[field] = value.trim();
        normCache.delete(student.name);
        reconstructTextarea();
        renderEngine();
    }
}

function removeStudent(id) {
    studentPool = studentPool.filter(s => s.id !== id);
    reconstructTextarea();
    renderEngine();
}

function reconstructTextarea() {
    const lines = studentPool.map(s => {
        if (s.mother) {
            return `${s.name}\t${s.mother}\t${s.grade}\t${s.section}${s.guardian ? `\t${s.guardian}` : ''}`;
        } else {
            return `${s.name}\t${s.grade}\t${s.section}`;
        }
    });
    document.getElementById('namesInput').value = lines.join('\n');
    onInputChanged();
}

function filterByGrade() {
    const selectedGrade = document.getElementById('gradeFilter').value;
    const dict = translations[currentLang];

    if (selectedGrade === 'all') {
        displayResults();
        return;
    }

    const resultsArea = document.getElementById('resultsArea');
    const resultsCount = document.getElementById('resultsCount');

    if (activeTab === 'siblings') {
        const filteredGroups = siblingResults.filter(group => {
            return group.students.some(s => s.grade === selectedGrade);
        });
        resultsCount.textContent = `${filteredGroups.length} ${dict.badge_groups_sib}`;
        resultsArea.className = "flex-grow space-y-3 overflow-y-auto max-h-[580px] custom-scroll pl-1 pr-1";
        renderSiblingsList(filteredGroups);
    } else if (activeTab === 'relatives') {
        const filteredGroups = relativeResults.filter(group => {
            return Object.values(group.branches).some(branch => branch.some(s => s.grade === selectedGrade));
        });
        resultsCount.textContent = `${filteredGroups.length} ${dict.badge_groups_rel}`;
        resultsArea.className = "flex-grow space-y-3 overflow-y-auto max-h-[580px] custom-scroll pl-1 pr-1";
        renderRelativesList(filteredGroups);
    } else if (activeTab === 'solitary') {
        const filteredStudents = solitaryResults.filter(s => s.grade === selectedGrade);
        resultsCount.textContent = `${filteredStudents.length} ${dict.badge_groups_sol}`;
        resultsArea.className = "flex-grow space-y-3 overflow-y-auto max-h-[580px] custom-scroll pl-1 pr-1";
        renderSolitaryList(filteredStudents);
    }
}

function exportToCSV() {
    if (studentPool.length === 0) return;
    const dict = translations[currentLang];
    let csvContent = "data:text/csv;charset=utf-8,\uFEFF"; 
    
    if (activeTab === 'siblings') {
        csvContent += `${dict.family_label},${dict.csv_name},${dict.mother_label},${dict.grade_label},${dict.section_label}\n`;
        siblingResults.forEach(group => {
            group.students.forEach(s => {
                csvContent += `"${group.sharedName}","${s.name}","${s.mother || ''}","${s.grade}","${s.section}"\n`;
            });
        });
    } else if (activeTab === 'relatives') {
        csvContent += `${dict.csv_type},${dict.csv_shared},${dict.csv_branch},${dict.csv_name},${dict.mother_label},${dict.grade_label},${dict.section_label}\n`;
        relativeResults.forEach(group => {
            const relType = group.type === 'maternal' ? dict.csv_maternal_type : dict.csv_paternal_type;
            Object.keys(group.branches).forEach(branch => {
                group.branches[branch].forEach(s => {
                    csvContent += `"${relType}","${group.sharedKey}","${branch}","${s.name}","${s.mother || ''}","${s.grade}","${s.section}"\n`;
                });
            });
        });
    } else if (activeTab === 'solitary') {
        csvContent += `${dict.csv_type},${dict.csv_name},${dict.mother_label},${dict.grade_label},${dict.section_label}\n`;
        solitaryResults.forEach(s => {
            csvContent += `"${dict.csv_solitary_type}","${s.name}","${s.mother || ''}","${s.grade}","${s.section}"\n`;
        });
    }

    const encodedUri = encodeURI(csvContent);
    const link = document.createElement("a");
    link.setAttribute("href", encodedUri);
    link.setAttribute("download", `school_${activeTab}_${currentLang}.csv`);
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    showToast(dict.btn_excel);
}

function printReport() {
    if (studentPool.length === 0) return;
    const dict = translations[currentLang];
    const titleEl = document.getElementById('printReportTitle');
    const metaEl = document.getElementById('printReportMeta');
    
    if (activeTab === 'siblings') {
        if (titleEl) titleEl.textContent = `📋 ${dict.tab_siblings}`;
        if (metaEl) metaEl.textContent = `${dict.badge_groups_sib}: ${siblingResults.length} | ${dict.stat_siblings}: ${siblingResults.reduce((acc, g) => acc + g.students.length, 0)} ${dict.students_suffix} | ${new Date().toLocaleDateString()}`;
    } else if (activeTab === 'relatives') {
        if (titleEl) titleEl.textContent = `🌿 ${dict.tab_relatives}`;
        if (metaEl) metaEl.textContent = `${dict.badge_groups_rel}: ${relativeResults.length} | ${dict.stat_relatives}: ${relativeResults.reduce((acc, g) => acc + g.totalCount, 0)} ${dict.students_suffix} | ${new Date().toLocaleDateString()}`;
    } else if (activeTab === 'solitary') {
        if (titleEl) titleEl.textContent = `👤 ${dict.tab_solitary}`;
        if (metaEl) metaEl.textContent = `${dict.badge_groups_sol}: ${solitaryResults.length} ${dict.students_suffix} | ${new Date().toLocaleDateString()}`;
    }
    window.print();
}

function copyFamilyCard(index) {
    const group = siblingResults[index];
    const dict = translations[currentLang];
    let text = `📋 ${dict.family_label} ${group.sharedName}\n`;
    group.students.forEach((s, idx) => {
        text += `${idx + 1}. ${s.name} - ${dict.grade_label} ${s.grade} - ${dict.section_label} ${s.section}${s.mother ? ` - ${dict.mother_label} ${s.mother}` : ''}\n`;
    });
    navigator.clipboard.writeText(text).then(() => {
        showToast(dict.copy_success);
    });
}

function copyRelativeCard(index) {
    const group = relativeResults[index];
    const dict = translations[currentLang];
    const relHeader = group.type === 'maternal' ? `🌸 ${dict.maternal_family_label} ${group.sharedKey}` : `🌿 ${dict.family_label} ${group.sharedKey}`;
    let text = `📋 ${relHeader} (${group.totalCount} ${dict.students_suffix})\n`;
    
    Object.keys(group.branches).forEach(branch => {
        text += `\n[${group.type === 'maternal' ? dict.branch_label : dict.father_branch} ${branch}]\n`;
        group.branches[branch].forEach((s, idx) => {
            text += `  - ${s.name} (${dict.grade_label} ${s.grade} / ${dict.section_label} ${s.section})\n`;
        });
    });

    navigator.clipboard.writeText(text).then(() => {
        showToast(dict.copy_success);
    });
}

function copySolitaryList() {
    const dict = translations[currentLang];
    let text = `📋 ${dict.singletons_title} (${solitaryResults.length} ${dict.students_suffix})\n`;
    solitaryResults.forEach((s, idx) => {
        text += `${idx + 1}. ${s.name} - ${dict.grade_label} ${s.grade} - ${dict.section_label} ${s.section}${s.mother ? ` - ${dict.mother_label} ${s.mother}` : ''}\n`;
    });
    navigator.clipboard.writeText(text).then(() => {
        showToast(dict.copy_success);
    });
}

function saveBackupLocally() {
    const rawInput = document.getElementById('namesInput').value;
    localStorage.setItem('siblings_backup', rawInput);
}

function updateHistoryArchive(data) {
    let history = JSON.parse(localStorage.getItem('siblings_history') || '[]');
    if (history.includes(data)) return;
    
    history.unshift(data);
    if (history.length > 5) history.pop(); 
    
    localStorage.setItem('siblings_history', JSON.stringify(history));
    renderHistoryUI();
}

function renderHistoryUI() {
    const historyList = document.getElementById('historyList');
    if (!historyList) return;
    const history = JSON.parse(localStorage.getItem('siblings_history') || '[]');
    historyList.innerHTML = '';
    
    history.forEach((data, index) => {
        const btn = document.createElement('button');
        btn.className = "clay-btn-sec px-1.5 py-0.2 rounded-lg text-[8px] font-bold";
        btn.textContent = `${translations[currentLang].history_ver} ${index + 1}`;
        btn.onclick = function() {
            document.getElementById('namesInput').value = data;
            onInputChanged();
            processNames();
        };
        historyList.appendChild(btn);
    });
}

function setTheme(color) {
    document.documentElement.className = '';
    document.documentElement.classList.add(`theme-${color}`);
    localStorage.setItem('siblings_theme', color);
}

// تبديل فوري وسلس بين الوضع الفاتح والداكن
function toggleDarkMode() {
    const root = document.documentElement;
    const isDark = root.classList.contains('theme-dark');
    root.className = '';
    const dict = translations[currentLang];
    
    if (!isDark) {
        root.classList.add('theme-dark');
        document.getElementById('darkBtnText').textContent = dict.dark_mode_light;
        localStorage.setItem('siblings_theme', 'dark');
    } else {
        setTheme('blue');
        document.getElementById('darkBtnText').textContent = dict.dark_mode_dark;
    }
}

// فلترة فورية مع خفض استهلاك المعالج
let searchDebounceTimer = null;
function filterResults() {
    clearTimeout(searchDebounceTimer);
    searchDebounceTimer = setTimeout(() => {
        const query = normalizeText(document.getElementById('searchFilter').value);
        const cards = document.querySelectorAll('.group-card');

        cards.forEach(card => {
            const searchPool = normalizeText(card.getAttribute('data-search-pool') || '');
            const sharedName = normalizeText(card.getAttribute('data-shared') || '');

            if (!query || searchPool.includes(query) || sharedName.includes(query)) {
                card.style.display = 'block';
            } else {
                card.style.display = 'none';
            }
        });
    }, 40);
}

function clearResultsArea() {
    const resultsArea = document.getElementById('resultsArea');
    const resultsCount = document.getElementById('resultsCount');
    const searchFilterContainer = document.getElementById('searchFilterContainer');
    const dict = translations[currentLang];

    resultsArea.className = "flex-grow flex flex-col justify-center items-center text-slate-400 py-10 text-center";
    resultsArea.innerHTML = `
        <svg xmlns="http://www.w3.org/2000/svg" class="h-14 w-14 mb-3 text-slate-300 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
        </svg>
        <p id="ui-results-default-desc" class="text-xs px-4 max-w-md leading-relaxed">${dict.results_default_desc}</p>`;
    
    resultsCount.classList.add('hidden');
    searchFilterContainer.classList.add('hidden');
    document.getElementById('gradeFilter').classList.add('hidden');

    document.getElementById('stat-total').textContent = '0';
    document.getElementById('stat-siblings').textContent = '0';
    document.getElementById('stat-relatives').textContent = '0';
    document.getElementById('stat-twins').textContent = '0';
    document.getElementById('stat-solitary').textContent = '0';
}

function clearAll() {
    document.getElementById('namesInput').value = '';
    document.getElementById('searchFilter').value = '';
    localStorage.removeItem('siblings_backup');
    normCache.clear();
    prefixCache.clear();
    studentPool = [];
    siblingResults = [];
    relativeResults = [];
    solitaryResults = [];
    onInputChanged();
    clearResultsArea();
    showToast(translations[currentLang].btn_clear);
}

// ميزات تثبيت التطبيق على أي جهاز (PWA Install Logic)
let deferredPrompt = null;
window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault();
    deferredPrompt = e;
    const pwaBtn = document.getElementById('pwaInstallBtn');
    if (pwaBtn) {
        pwaBtn.classList.remove('hidden');
        pwaBtn.classList.add('flex');
    }
});

window.addEventListener('appinstalled', () => {
    deferredPrompt = null;
    const pwaBtn = document.getElementById('pwaInstallBtn');
    if (pwaBtn) pwaBtn.classList.add('hidden');
    showToast(translations[currentLang].copy_success);
});

function installPWA() {
    if (deferredPrompt) {
        deferredPrompt.prompt();
        deferredPrompt.userChoice.then((choiceResult) => {
            if (choiceResult.outcome === 'accepted') {
                showToast(currentLang === 'en' ? 'App Installed!' : 'بەرنامە ب سەرکەفتی هاتە دانان!');
            }
            deferredPrompt = null;
            const pwaBtn = document.getElementById('pwaInstallBtn');
            if (pwaBtn) pwaBtn.classList.add('hidden');
        });
    } else {
        const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
        if (isIOS) {
            alert(currentLang === 'en' 
                ? 'To install: tap Share (⎋) and select "Add to Home Screen"' 
                : 'بۆ دانانێ: ل سەر دوگمەیا Share (⎋) د Safari دا کلیک بکە و پاشان "Add to Home Screen" هەلبژێرە.');
        } else {
            alert(currentLang === 'en' 
                ? 'To install: click your browser menu (⋮ or ⋯) and select "Install App" or "Install Page as App".' 
                : 'بۆ دانانا بەرنامەی: ژ مێنیویا براوزەری (⋮) کلیک بکە ل سەر (تثبيت التطبيق / Install App).');
        }
    }
}

// تسجيل Service Worker للعمل دون إنترنت
if ('serviceWorker' in navigator) {
    window.addEventListener('load', () => {
        navigator.serviceWorker.register('./sw.js').catch((err) => {
            console.log('SW registration note:', err);
        });
    });
}

window.onload = function() {
    const savedTheme = localStorage.getItem('siblings_theme') || 'blue';
    if (savedTheme === 'dark') {
        toggleDarkMode();
    } else {
        setTheme(savedTheme);
    }

    const backup = localStorage.getItem('siblings_backup');
    if (backup) {
        document.getElementById('namesInput').value = backup;
        onInputChanged();
    }

    changeLanguage('ku_ba'); 
    
    if (backup) {
        processNames();
    }
};

window.onclick = function(event) {
    if (!event.target.matches('#langBtn') && !event.target.closest('#langMenu') && !event.target.closest('#langBtn')) {
        document.getElementById('langMenu')?.classList.add('hidden');
    }
};

